[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "astra-sdk"
version = "1.0.0"
description = "Python client SDK for the Astra X agent API"
requires-python = ">=3.11"
dependencies = ["httpx>=0.27"]
